{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Zensical: Servidor",
            "type": "node-terminal",
            "request": "launch",
            "command": "zensical serve --dev-addr 0.0.0.0:8000",
            "cwd": "${workspaceFolder}"
        }
    ]
}
